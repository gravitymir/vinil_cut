# Rust build output (root crate and the send-note sub-crate)
target/

# Per-render working dir and cached tinted discs
work/

# Telegram userbot session (grants access to your account — never commit)
*.session*

# Assets generated at startup (regenerated on first run)
assets/shine.png
assets/mask.png
assets/spindle.png
assets/font.ttf
assets/thumb_vinyl.png
assets/thumb_reel.png

# Source images kept only for reference (shipped bases: disc_base.png, reel_base.png)
assets/disc_source.png
assets/reel_source.png
